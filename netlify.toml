[build]
  command = "npm run build"  # or "yarn build", depending on your setup
  publish = "dist"          # or "dist" for Vite-based projects

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
